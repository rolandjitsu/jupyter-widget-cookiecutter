describe('{{ cookiecutter.npm_package_name }}', () => {
	// TODO: Apparently we cannot import the views or models because of some jQuery error, fix that
	it('should pass sanity check', () => {
		expect(true).toBeTruthy();
	});
});
